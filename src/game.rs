pub fn load_images(sources: Vec<String>, callback: Box<(FnOnce(Vec<String>) -> ())>) {
    unimplemented!()
}

#[derive(Clone, Copy)]
pub struct Runner {
    pub width: u32,
    pub height: u32,
}

impl Runner {
    pub fn start(self) {
        unimplemented!();
    }
}
